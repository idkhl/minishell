// exit les bons code

// export 
//			pas de chiffres  symboles avant =
// echo dans un fichier chmod000 ca echo quand meme
//		enlever le exit dans indfile dans execcmd exit que si pipe
// ctrl-\ signal+exit status


// expand dans le heredoc
//export u="s -l"

// 2 messages d'erreur infile n'existe pas
// << eof << eof ^C doit quitter tous les heredoc d'un coup

//free(str) dans fill_cmd_next?
// exit codes erreurs parsing
// fichier inexistant dans pipes -> echo $? = 0?

bash-5.1$ ls |< wc
bash: wc: No such file or directory
bash-5.1$ echo $?
0
bash-5.1$ ls |< infile kcn
bash: kcn: command not found
bash-5.1$ echo $?
127
bash-5.1$ ls |< knr wc
bash: knr: No such file or directory
bash-5.1$ echo $?
1

reverif exits + msg d'erreur << yo cat > 1 | << yo cat > 2 | << yo catiperry > 3
